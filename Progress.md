# Progress Updates

**[08/11/2024]** **3h30min**
- Changed protectVoucher to protect a JsonObject instead of .json file.
- Started rewriting the client in Java (`info` and `users` endpoints).
- Added simple `login/logout` capabilities to the client.